import { ROBOT_SCENE_URL } from '@/lib/hero-assets';

export default function Head() {
  return (
    <>
      {/* Spline assets removed from critical path to reduce TBT. Will load on interaction. */}
    </>
  );
}

